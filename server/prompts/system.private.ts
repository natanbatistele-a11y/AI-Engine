/**
 * Backend-only fallback system prompt.
 *
 * Replace the text below with your production system prompt **locally**.
 * Never commit real prompts or proprietary knowledge to source control.
 */
export const fallbackSystemPrompt = `You are a focused and concise AI assistant. Follow brand tone and keep responses actionable.`;
